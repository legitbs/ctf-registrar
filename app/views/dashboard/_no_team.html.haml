#no_team
  %p You're not on a team yet. Create or join one to compete.
  .join_team
    %h2 Join a team
    = form_for @new_membership, url: membership_path do |f|
      .field
        = f.label :name, 'Team Name'
        = f.text_field :name
      .field
        = f.label :password
        = f.password_field :password
        .explanation
          Get this from the team captain.
      .field#nolabel
        = f.submit 'Join'
  .create_team
    %h2 Create a team
    = form_for @new_team, url: team_path, html: {multipart: true} do |f|
      = render partial: 'teams/form', locals: {f: f}
      .field
        = f.submit 'Create Team'
