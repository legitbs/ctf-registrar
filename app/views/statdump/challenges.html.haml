%h1 all challenges
- Category.order(order: :asc).each do |cat|
  %h2= cat.name
  %ul
    - cat.challenges.order(points: :asc, name: :asc).each do |ch|
      %li
        %a{href: "challenges/#{ch.id}.html"} #{ch.points}: #{ch.name}
        
